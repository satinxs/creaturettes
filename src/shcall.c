#include <stdio.h>
#include <stdlib.h>
// #include <stdbool.h>

#define bool _Bool
#define true 1
#define false 0

bool sh(char *str, ...) {
    // static Cmd cmd = {0};

    // cmd.count = 0;
    // cmd_append(&cmd, str);
    // return cmd_run_sync(cmd);
}
